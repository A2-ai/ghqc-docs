---
title: Review the QC Files
description: "Initial walkthrough of the review functionality"
---

import reviewDirtyFile from '../../../../assets/intro/getting-started/review/dirty-file.png';
import reviewPopUp from '../../../../assets/intro/getting-started/review/pop-up.png';
import reviewPreview from '../../../../assets/intro/getting-started/review/preview.png';

As the reviewer, we recommend using the review workflow built into `ghqc` to leave comments. It keeps the QC discussion organized, preserves traceability, and lets you leave
inline comments directly in the file. To do this, open the file locally and add comments where you want them. `ghqc` reads the local file content, compares it to the latest
committed version, and includes the diff directly in the issue comment body.

Continuing the `scripts/helper.R` example, the file simply adds two numbers and returns their sum. The reviewer wants to point out that the explicit `return()` is not needed,
since R returns the last expression by default. They can leave that comment directly in the file and save it locally.

```r ins={3}
//helper.R 
add2 <- function(a, b) {
    # QC: No need for return, R will return value by default
    return(a + b)
}
```

After editing the file, the issue will be marked with a star once the Git status refreshes, indicating that there are local, uncommitted changes to the file.

<div style="display: flex; justify-content: center;">
    <img src={reviewDirtyFile.src} style="width: 75%;" />
</div>

Clicking the card opens a pop-up on the _Review_ tab. There, you can select the commit the review applies to, include an optional comment, and preview the comment that will be posted.

<div style="display: flex; justify-content: center;">
    <img src={reviewPopUp.src} style="width: 60%;" />
</div>

<div style="display: flex; justify-content: center;">
    <img src={reviewPreview.src} style="width: 60%;" />
</div>

Clicking _Post_ posts the comment and updates the issue status to _Findings to Address_.

:::note
After posting a review, the file will remain in a dirty state because your local comment is still present. To return the file to its last committed state, run one of the following commands:
```bash
git checkout -- <reviewed file>
# OR
git restore <reviewed file>
```
:::
